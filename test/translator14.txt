
ibcp  Copyright (C) 2010-2012  Thunder422
This program comes with ABSOLUTELY NO WARRANTY.
This is free software, and you are welcome to
redistribute it under certain conditions.

Table initialization successful.

Input: ?
       ^-- PARSER: unrecognizable character

Input: 01
       ^-- PARSER: invalid leading zero in numeric constant

Input: ..
       ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: .e
       ^-- PARSER: mantissa of constant only contains a decimal point

Input: 1e
         ^-- PARSER: exponent contains no digits

Input: .
       ^-- PARSER: constant only contains a decimal point

Input: 1e999
       ^^^^^-- PARSER: constant is out of range

Input: A?
        ^-- PARSER: unrecognizable character

Input: A 01
         ^-- PARSER: invalid leading zero in numeric constant

Input: A..
        ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A.e
        ^-- PARSER: mantissa of constant only contains a decimal point

Input: A 1e
           ^-- PARSER: exponent contains no digits

Input: A.
        ^-- PARSER: constant only contains a decimal point

Input: A 1e999
         ^^^^^-- PARSER: constant is out of range

Input: A(?
         ^-- PARSER: unrecognizable character

Input: A(01
         ^-- PARSER: invalid leading zero in numeric constant

Input: A(..
         ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A(.e
         ^-- PARSER: mantissa of constant only contains a decimal point

Input: A(1e
           ^-- PARSER: exponent contains no digits

Input: A(.
         ^-- PARSER: constant only contains a decimal point

Input: A(1e999
         ^^^^^-- PARSER: constant is out of range

Input: A(1?
          ^-- PARSER: unrecognizable character

Input: A(1 01
           ^-- PARSER: invalid leading zero in numeric constant

Input: A(1 ..
           ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A(1 .e
           ^-- PARSER: mantissa of constant only contains a decimal point

Input: A(1 1e
             ^-- PARSER: exponent contains no digits

Input: A(1 .
           ^-- PARSER: constant only contains a decimal point

Input: A(1 1e999
           ^^^^^-- PARSER: constant is out of range

Input: A(1,?
           ^-- PARSER: unrecognizable character

Input: A(1,01
           ^-- PARSER: invalid leading zero in numeric constant

Input: A(1,..
           ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A(1,.e
           ^-- PARSER: mantissa of constant only contains a decimal point

Input: A(1,1e
             ^-- PARSER: exponent contains no digits

Input: A(1,.
           ^-- PARSER: constant only contains a decimal point

Input: A(1,1e999
           ^^^^^-- PARSER: constant is out of range

Input: A,?
         ^-- PARSER: unrecognizable character

Input: A,01
         ^-- PARSER: invalid leading zero in numeric constant

Input: A,..
         ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A,.e
         ^-- PARSER: mantissa of constant only contains a decimal point

Input: A,1e
           ^-- PARSER: exponent contains no digits

Input: A,.
         ^-- PARSER: constant only contains a decimal point

Input: A,1e999
         ^^^^^-- PARSER: constant is out of range

Input: A,B?
          ^-- PARSER: unrecognizable character

Input: A,B 01
           ^-- PARSER: invalid leading zero in numeric constant

Input: A,B..
          ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A,B.e
          ^-- PARSER: mantissa of constant only contains a decimal point

Input: A,B 1e
             ^-- PARSER: exponent contains no digits

Input: A,B.
          ^-- PARSER: constant only contains a decimal point

Input: A,B 1e999
           ^^^^^-- PARSER: constant is out of range

Input: A,B,?
           ^-- PARSER: unrecognizable character

Input: A,B,01
           ^-- PARSER: invalid leading zero in numeric constant

Input: A,B,..
           ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A,B,.e
           ^-- PARSER: mantissa of constant only contains a decimal point

Input: A,B,1e
             ^-- PARSER: exponent contains no digits

Input: A,B,.
           ^-- PARSER: constant only contains a decimal point

Input: A,B,1e999
           ^^^^^-- PARSER: constant is out of range

Input: A=?
         ^-- PARSER: unrecognizable character

Input: A=01
         ^-- PARSER: invalid leading zero in numeric constant

Input: A=..
         ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A=.e
         ^-- PARSER: mantissa of constant only contains a decimal point

Input: A=1e
           ^-- PARSER: exponent contains no digits

Input: A=.
         ^-- PARSER: constant only contains a decimal point

Input: A=1e999
         ^^^^^-- PARSER: constant is out of range

Input: A=1?
          ^-- PARSER: unrecognizable character

Input: A=1 01
           ^-- PARSER: invalid leading zero in numeric constant

Input: A=1 ..
           ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A=1 .e
           ^-- PARSER: mantissa of constant only contains a decimal point

Input: A=1 1e
             ^-- PARSER: exponent contains no digits

Input: A=1 .
           ^-- PARSER: constant only contains a decimal point

Input: A=1 1e999
           ^^^^^-- PARSER: constant is out of range

Input: A=1+?
           ^-- PARSER: unrecognizable character

Input: A=1+01
           ^-- PARSER: invalid leading zero in numeric constant

Input: A=1+..
           ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A=1+.e
           ^-- PARSER: mantissa of constant only contains a decimal point

Input: A=1+1e
             ^-- PARSER: exponent contains no digits

Input: A=1+.
           ^-- PARSER: constant only contains a decimal point

Input: A=1+1e999
           ^^^^^-- PARSER: constant is out of range

Input: A$=?
          ^-- PARSER: unrecognizable character

Input: A$=01
          ^-- PARSER: invalid leading zero in numeric constant

Input: A$=..
          ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A$=.e
          ^-- PARSER: mantissa of constant only contains a decimal point

Input: A$=1e
            ^-- PARSER: exponent contains no digits

Input: A$=.
          ^-- PARSER: constant only contains a decimal point

Input: A$=1e999
          ^^^^^-- PARSER: constant is out of range

Input: A$="A"?
             ^-- PARSER: unrecognizable character

Input: A$="A" 01
              ^-- PARSER: invalid leading zero in numeric constant

Input: A$="A" ..
              ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A$="A" .e
              ^-- PARSER: mantissa of constant only contains a decimal point

Input: A$="A" 1e
                ^-- PARSER: exponent contains no digits

Input: A$="A" .
              ^-- PARSER: constant only contains a decimal point

Input: A$="A" 1e999
              ^^^^^-- PARSER: constant is out of range

Input: A$="A"+?
              ^-- PARSER: unrecognizable character

Input: A$="A"+01
              ^-- PARSER: invalid leading zero in numeric constant

Input: A$="A"+..
              ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A$="A"+.e
              ^-- PARSER: mantissa of constant only contains a decimal point

Input: A$="A"+1e
                ^-- PARSER: exponent contains no digits

Input: A$="A"+.
              ^-- PARSER: constant only contains a decimal point

Input: A$="A"+1e999
              ^^^^^-- PARSER: constant is out of range

Input: MID$(?
            ^-- PARSER: unrecognizable character

Input: MID$(01
            ^-- PARSER: invalid leading zero in numeric constant

Input: MID$(..
            ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: MID$(.e
            ^-- PARSER: mantissa of constant only contains a decimal point

Input: MID$(1e
              ^-- PARSER: exponent contains no digits

Input: MID$(.
            ^-- PARSER: constant only contains a decimal point

Input: MID$(1e999
            ^^^^^-- PARSER: constant is out of range

Input: MID$(A$?
              ^-- PARSER: unrecognizable character

Input: MID$(A$01
              ^-- PARSER: invalid leading zero in numeric constant

Input: MID$(A$..
              ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: MID$(A$.e
              ^-- PARSER: mantissa of constant only contains a decimal point

Input: MID$(A$1e
                ^-- PARSER: exponent contains no digits

Input: MID$(A$.
              ^-- PARSER: constant only contains a decimal point

Input: MID$(A$1e999
              ^^^^^-- PARSER: constant is out of range

Input: MID$(A$,?
               ^-- PARSER: unrecognizable character

Input: MID$(A$,01
               ^-- PARSER: invalid leading zero in numeric constant

Input: MID$(A$,..
               ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: MID$(A$,.e
               ^-- PARSER: mantissa of constant only contains a decimal point

Input: MID$(A$,1e
                 ^-- PARSER: exponent contains no digits

Input: MID$(A$,.
               ^-- PARSER: constant only contains a decimal point

Input: MID$(A$,1e999
               ^^^^^-- PARSER: constant is out of range

Input: MID$(A$,3?
                ^-- PARSER: unrecognizable character

Input: MID$(A$,3 01
                 ^-- PARSER: invalid leading zero in numeric constant

Input: MID$(A$,3 ..
                 ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: MID$(A$,3 .e
                 ^-- PARSER: mantissa of constant only contains a decimal point

Input: MID$(A$,3 1e
                   ^-- PARSER: exponent contains no digits

Input: MID$(A$,3 .
                 ^-- PARSER: constant only contains a decimal point

Input: MID$(A$,3 1e999
                 ^^^^^-- PARSER: constant is out of range

Input: MID$(A$,3,?
                 ^-- PARSER: unrecognizable character

Input: MID$(A$,3,01
                 ^-- PARSER: invalid leading zero in numeric constant

Input: MID$(A$,3,..
                 ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: MID$(A$,3,.e
                 ^-- PARSER: mantissa of constant only contains a decimal point

Input: MID$(A$,3,1e
                   ^-- PARSER: exponent contains no digits

Input: MID$(A$,3,.
                 ^-- PARSER: constant only contains a decimal point

Input: MID$(A$,3,1e999
                 ^^^^^-- PARSER: constant is out of range

Input: MID$(A$,3,4?
                  ^-- PARSER: unrecognizable character

Input: MID$(A$,3,4 01
                   ^-- PARSER: invalid leading zero in numeric constant

Input: MID$(A$,3,4 ..
                   ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: MID$(A$,3,4 .e
                   ^-- PARSER: mantissa of constant only contains a decimal point

Input: MID$(A$,3,4 1e
                     ^-- PARSER: exponent contains no digits

Input: MID$(A$,3,4 .
                   ^-- PARSER: constant only contains a decimal point

Input: MID$(A$,3,4 1e999
                   ^^^^^-- PARSER: constant is out of range

Input: A$=MID$(?
               ^-- PARSER: unrecognizable character

Input: A$=MID$(01
               ^-- PARSER: invalid leading zero in numeric constant

Input: A$=MID$(..
               ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A$=MID$(.e
               ^-- PARSER: mantissa of constant only contains a decimal point

Input: A$=MID$(1e
                 ^-- PARSER: exponent contains no digits

Input: A$=MID$(.
               ^-- PARSER: constant only contains a decimal point

Input: A$=MID$(1e999
               ^^^^^-- PARSER: constant is out of range

Input: A$=MID$(B$?
                 ^-- PARSER: unrecognizable character

Input: A$=MID$(B$01
                 ^-- PARSER: invalid leading zero in numeric constant

Input: A$=MID$(B$..
                 ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A$=MID$(B$.e
                 ^-- PARSER: mantissa of constant only contains a decimal point

Input: A$=MID$(B$1e
                   ^-- PARSER: exponent contains no digits

Input: A$=MID$(B$.
                 ^-- PARSER: constant only contains a decimal point

Input: A$=MID$(B$1e999
                 ^^^^^-- PARSER: constant is out of range

Input: A$=MID$(B$,?
                  ^-- PARSER: unrecognizable character

Input: A$=MID$(B$,01
                  ^-- PARSER: invalid leading zero in numeric constant

Input: A$=MID$(B$,..
                  ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A$=MID$(B$,.e
                  ^-- PARSER: mantissa of constant only contains a decimal point

Input: A$=MID$(B$,1e
                    ^-- PARSER: exponent contains no digits

Input: A$=MID$(B$,.
                  ^-- PARSER: constant only contains a decimal point

Input: A$=MID$(B$,1e999
                  ^^^^^-- PARSER: constant is out of range

Input: A$=MID$(B$,3?
                   ^-- PARSER: unrecognizable character

Input: A$=MID$(B$,3 01
                    ^-- PARSER: invalid leading zero in numeric constant

Input: A$=MID$(B$,3 ..
                    ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A$=MID$(B$,3 .e
                    ^-- PARSER: mantissa of constant only contains a decimal point

Input: A$=MID$(B$,3 1e
                      ^-- PARSER: exponent contains no digits

Input: A$=MID$(B$,3 .
                    ^-- PARSER: constant only contains a decimal point

Input: A$=MID$(B$,3 1e999
                    ^^^^^-- PARSER: constant is out of range

Input: A$=MID$(B$,3,?
                    ^-- PARSER: unrecognizable character

Input: A$=MID$(B$,3,01
                    ^-- PARSER: invalid leading zero in numeric constant

Input: A$=MID$(B$,3,..
                    ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A$=MID$(B$,3,.e
                    ^-- PARSER: mantissa of constant only contains a decimal point

Input: A$=MID$(B$,3,1e
                      ^-- PARSER: exponent contains no digits

Input: A$=MID$(B$,3,.
                    ^-- PARSER: constant only contains a decimal point

Input: A$=MID$(B$,3,1e999
                    ^^^^^-- PARSER: constant is out of range

Input: A$=MID$(B$,3,4?
                     ^-- PARSER: unrecognizable character

Input: A$=MID$(B$,3,4 01
                      ^-- PARSER: invalid leading zero in numeric constant

Input: A$=MID$(B$,3,4 ..
                      ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: A$=MID$(B$,3,4 .e
                      ^-- PARSER: mantissa of constant only contains a decimal point

Input: A$=MID$(B$,3,4 1e
                        ^-- PARSER: exponent contains no digits

Input: A$=MID$(B$,3,4 .
                      ^-- PARSER: constant only contains a decimal point

Input: A$=MID$(B$,3,4 1e999
                      ^^^^^-- PARSER: constant is out of range

Input: PRINT ?
             ^-- PARSER: unrecognizable character

Input: PRINT 01
             ^-- PARSER: invalid leading zero in numeric constant

Input: PRINT ..
             ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: PRINT .e
             ^-- PARSER: mantissa of constant only contains a decimal point

Input: PRINT 1e
               ^-- PARSER: exponent contains no digits

Input: PRINT .
             ^-- PARSER: constant only contains a decimal point

Input: PRINT 1e999
             ^^^^^-- PARSER: constant is out of range

Input: PRINT A?
              ^-- PARSER: unrecognizable character

Input: PRINT A 01
               ^-- PARSER: invalid leading zero in numeric constant

Input: PRINT A..
              ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: PRINT A.e
              ^-- PARSER: mantissa of constant only contains a decimal point

Input: PRINT A 1e
                 ^-- PARSER: exponent contains no digits

Input: PRINT A.
              ^-- PARSER: constant only contains a decimal point

Input: PRINT A 1e999
               ^^^^^-- PARSER: constant is out of range

Input: PRINT A(?
               ^-- PARSER: unrecognizable character

Input: PRINT A(01
               ^-- PARSER: invalid leading zero in numeric constant

Input: PRINT A(..
               ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: PRINT A(.e
               ^-- PARSER: mantissa of constant only contains a decimal point

Input: PRINT A(1e
                 ^-- PARSER: exponent contains no digits

Input: PRINT A(.
               ^-- PARSER: constant only contains a decimal point

Input: PRINT A(1e999
               ^^^^^-- PARSER: constant is out of range

Input: PRINT A(1?
                ^-- PARSER: unrecognizable character

Input: PRINT A(1 01
                 ^-- PARSER: invalid leading zero in numeric constant

Input: PRINT A(1 ..
                 ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: PRINT A(1 .e
                 ^-- PARSER: mantissa of constant only contains a decimal point

Input: PRINT A(1 1e
                   ^-- PARSER: exponent contains no digits

Input: PRINT A(1 .
                 ^-- PARSER: constant only contains a decimal point

Input: PRINT A(1 1e999
                 ^^^^^-- PARSER: constant is out of range

Input: PRINT A(1,?
                 ^-- PARSER: unrecognizable character

Input: PRINT A(1,01
                 ^-- PARSER: invalid leading zero in numeric constant

Input: PRINT A(1,..
                 ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: PRINT A(1,.e
                 ^-- PARSER: mantissa of constant only contains a decimal point

Input: PRINT A(1,1e
                   ^-- PARSER: exponent contains no digits

Input: PRINT A(1,.
                 ^-- PARSER: constant only contains a decimal point

Input: PRINT A(1,1e999
                 ^^^^^-- PARSER: constant is out of range

Input: PRINT A,?
               ^-- PARSER: unrecognizable character

Input: PRINT A,01
               ^-- PARSER: invalid leading zero in numeric constant

Input: PRINT A,..
               ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: PRINT A,.e
               ^-- PARSER: mantissa of constant only contains a decimal point

Input: PRINT A,1e
                 ^-- PARSER: exponent contains no digits

Input: PRINT A,.
               ^-- PARSER: constant only contains a decimal point

Input: PRINT A,1e999
               ^^^^^-- PARSER: constant is out of range

Input: PRINT A,,?
                ^-- PARSER: unrecognizable character

Input: PRINT A,,01
                ^-- PARSER: invalid leading zero in numeric constant

Input: PRINT A,,..
                ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: PRINT A,,.e
                ^-- PARSER: mantissa of constant only contains a decimal point

Input: PRINT A,,1e
                  ^-- PARSER: exponent contains no digits

Input: PRINT A,,.
                ^-- PARSER: constant only contains a decimal point

Input: PRINT A,,1e999
                ^^^^^-- PARSER: constant is out of range

Input: INPUT ?
             ^-- PARSER: unrecognizable character

Input: INPUT 01
             ^-- PARSER: invalid leading zero in numeric constant

Input: INPUT ..
             ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: INPUT .e
             ^-- PARSER: mantissa of constant only contains a decimal point

Input: INPUT 1e
               ^-- PARSER: exponent contains no digits

Input: INPUT .
             ^-- PARSER: constant only contains a decimal point

Input: INPUT 1e999
             ^^^^^-- PARSER: constant is out of range

Input: INPUT A?
              ^-- PARSER: unrecognizable character

Input: INPUT A 01
               ^-- PARSER: invalid leading zero in numeric constant

Input: INPUT A..
              ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: INPUT A.e
              ^-- PARSER: mantissa of constant only contains a decimal point

Input: INPUT A 1e
                 ^-- PARSER: exponent contains no digits

Input: INPUT A.
              ^-- PARSER: constant only contains a decimal point

Input: INPUT A 1e999
               ^^^^^-- PARSER: constant is out of range

Input: INPUT A,?
               ^-- PARSER: unrecognizable character

Input: INPUT A,01
               ^-- PARSER: invalid leading zero in numeric constant

Input: INPUT A,..
               ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: INPUT A,.e
               ^-- PARSER: mantissa of constant only contains a decimal point

Input: INPUT A,1e
                 ^-- PARSER: exponent contains no digits

Input: INPUT A,.
               ^-- PARSER: constant only contains a decimal point

Input: INPUT A,1e999
               ^^^^^-- PARSER: constant is out of range

Input: INPUT A(1,?
                 ^-- PARSER: unrecognizable character

Input: INPUT A(1,01
                 ^-- PARSER: invalid leading zero in numeric constant

Input: INPUT A(1,..
                 ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: INPUT A(1,.e
                 ^-- PARSER: mantissa of constant only contains a decimal point

Input: INPUT A(1,1e
                   ^-- PARSER: exponent contains no digits

Input: INPUT A(1,.
                 ^-- PARSER: constant only contains a decimal point

Input: INPUT A(1,1e999
                 ^^^^^-- PARSER: constant is out of range

Input: INPUT PROMPT ?
                    ^-- PARSER: unrecognizable character

Input: INPUT PROMPT 01
                    ^-- PARSER: invalid leading zero in numeric constant

Input: INPUT PROMPT ..
                    ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: INPUT PROMPT .e
                    ^-- PARSER: mantissa of constant only contains a decimal point

Input: INPUT PROMPT 1e
                      ^-- PARSER: exponent contains no digits

Input: INPUT PROMPT .
                    ^-- PARSER: constant only contains a decimal point

Input: INPUT PROMPT 1e999
                    ^^^^^-- PARSER: constant is out of range

Input: INPUT PROMPT "A"?
                       ^-- PARSER: unrecognizable character

Input: INPUT PROMPT "A"01
                       ^-- PARSER: invalid leading zero in numeric constant

Input: INPUT PROMPT "A"..
                       ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: INPUT PROMPT "A".e
                       ^-- PARSER: mantissa of constant only contains a decimal point

Input: INPUT PROMPT "A"1e
                         ^-- PARSER: exponent contains no digits

Input: INPUT PROMPT "A".
                       ^-- PARSER: constant only contains a decimal point

Input: INPUT PROMPT "A"1e999
                       ^^^^^-- PARSER: constant is out of range

Input: INPUT PROMPT "A",?
                        ^-- PARSER: unrecognizable character

Input: INPUT PROMPT "A",01
                        ^-- PARSER: invalid leading zero in numeric constant

Input: INPUT PROMPT "A",..
                        ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: INPUT PROMPT "A",.e
                        ^-- PARSER: mantissa of constant only contains a decimal point

Input: INPUT PROMPT "A",1e
                          ^-- PARSER: exponent contains no digits

Input: INPUT PROMPT "A",.
                        ^-- PARSER: constant only contains a decimal point

Input: INPUT PROMPT "A",1e999
                        ^^^^^-- PARSER: constant is out of range

Input: INPUT PROMPT "A",A?
                         ^-- PARSER: unrecognizable character

Input: INPUT PROMPT "A",A 01
                          ^-- PARSER: invalid leading zero in numeric constant

Input: INPUT PROMPT "A",A..
                         ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: INPUT PROMPT "A",A.e
                         ^-- PARSER: mantissa of constant only contains a decimal point

Input: INPUT PROMPT "A",A 1e
                            ^-- PARSER: exponent contains no digits

Input: INPUT PROMPT "A",A.
                         ^-- PARSER: constant only contains a decimal point

Input: INPUT PROMPT "A",A 1e999
                          ^^^^^-- PARSER: constant is out of range

Input: INPUT PROMPT A$(1,?
                         ^-- PARSER: unrecognizable character

Input: INPUT PROMPT A$(1,01
                         ^-- PARSER: invalid leading zero in numeric constant

Input: INPUT PROMPT A$(1,..
                         ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: INPUT PROMPT A$(1,.e
                         ^-- PARSER: mantissa of constant only contains a decimal point

Input: INPUT PROMPT A$(1,1e
                           ^-- PARSER: exponent contains no digits

Input: INPUT PROMPT A$(1,.
                         ^-- PARSER: constant only contains a decimal point

Input: INPUT PROMPT A$(1,1e999
                         ^^^^^-- PARSER: constant is out of range

Input: INPUT PROMPT "A";A(1,?
                            ^-- PARSER: unrecognizable character

Input: INPUT PROMPT "A";A(1,01
                            ^-- PARSER: invalid leading zero in numeric constant

Input: INPUT PROMPT "A";A(1,..
                            ^^-- PARSER: constant only contains a decimal point or has two decimal points

Input: INPUT PROMPT "A";A(1,.e
                            ^-- PARSER: mantissa of constant only contains a decimal point

Input: INPUT PROMPT "A";A(1,1e
                              ^-- PARSER: exponent contains no digits

Input: INPUT PROMPT "A";A(1,.
                            ^-- PARSER: constant only contains a decimal point

Input: INPUT PROMPT "A";A(1,1e999
                            ^^^^^-- PARSER: constant is out of range

